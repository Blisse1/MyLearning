## Comparators and Equality

### Difference between === and ==

- Basically the difference is that with the use of === we're also checking that **data types** are matching. And with == it doesn't check the **data type**.
- With === we say something like: "this value must be number and be a number"

### Arrays

